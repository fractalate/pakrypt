import styling from '../lib/styling'

export default function TileClosePak() {
  return <div className={styling.tile.tileComponentCommand}>
    <button className={styling.button.formButton}>Close Pak</button>
  </div>
}
