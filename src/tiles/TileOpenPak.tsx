import styling from '../lib/styling'

export default function TileOpenPak() {
  return <div className={styling.tile.tileComponentCommand}>
    <button className={styling.button.formButton}>Open Pak</button>
  </div>
}
