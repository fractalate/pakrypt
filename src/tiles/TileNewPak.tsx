import styling from '../lib/styling'

export default function TileNewPak() {
  return <div className={styling.tile.tileComponentCommand}>
    <button className={styling.button.formButton}>New Pak</button>
  </div>
}
